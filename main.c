#include "shell.h"

/**
 * main - Entry point.
 *
 * Return: Always 0.
 */
int main(int argc, char **argv)
{
	(void)argv; (void)argv;

	char *prompt = ">>";
	
	printf("%s\n", prompt);

	return (0);
}
